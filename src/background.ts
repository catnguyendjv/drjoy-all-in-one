/**
 * @file This is the background script for the extension.
 * It runs in the background and can listen for browser events.
 * Currently, it only logs a message to the console.
 */
export {}
console.log("HELLO WORLD FROM BGSCRIPTS")
